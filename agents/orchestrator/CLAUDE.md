# Orchestrator

The Orchestrator uses the repository root `CLAUDE.md` as its system prompt.

When you open a project built with Forge in Claude Code, Claude reads the root `CLAUDE.md` automatically and takes on the Orchestrator role. This directory is intentionally a placeholder so the agents directory is symmetrical.

See `../../CLAUDE.md` for the actual prompt.

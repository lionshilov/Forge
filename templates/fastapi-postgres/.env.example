# Async SQLAlchemy URL — matches the postgres service in docker-compose.yml
DATABASE_URL=postgresql+asyncpg://postgres:postgres@localhost:5432/app

# Enables SQL echo and debug behavior — keep false in production
DEBUG=false
